# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build artifacts (never commit these)
target/
old-builds/
*.exe
src-tauri/target/
dist/
*.nsis
*.wix
*.msi
bundle/

# Brain feed signing key (creator-only; never commit)
brain-feed/private.key.pem
brain-feed/private.key*

# Alpha tester API keys (creator-only; never commit)
scripts/alpha-keys.local.json

# Web deploy secrets (creator-only; never commit)
workers/.dev.vars
workers/.wrangler/
workers/cookie*.txt
workers/secrets-bulk.json

# Web build output (regenerated by scripts/build-web.ps1)
web-dist/

# MSIX local packaging (generated)
msix/dist/
msix/devcert.pfx
msix/*.msix

# Editor / internal creator files (not for public repo)
.cursor/
OUTREACH-EMAILS.txt
MARKETING-NOTES.txt
GRADE-LEVEL-NOTES.txt
copyright-deposit/
